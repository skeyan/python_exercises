#Name: Eva Yan
#Date: due Oct 16, 2018
#This program builds a circuit that has the same behavior as a nand gate
#Program 23 Computes NAND of two inputs, using only AND, OR, and NOT gates

#if both inputs are true, then false


out = (not (in1 and in2))
